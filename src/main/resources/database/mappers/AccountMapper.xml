<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winter.app.account.AccountDAO">

	<sql id="search">
		<where>
			<if test="kind=='kind1' or kind =='kind4'">
			PRODUCTNAME LIKE '%'||#{search}||'%'
			</if>
			<if test="kind=='kind2' or kind=='kind4'">
			OR ACCOUNTNUM  LIKE '%'||#{search}||'%'
			</if>
			<if test="kind=='kind3' or kind=='kind4'">
			OR ACCOUNTDATE  LIKE '%'||#{search}||'%'
			</if>
			OR A.USERNAME = #{userName}		
		</where>	
	</sql>
	

	<select id="getList" resultMap="getListResult" parameterType="Pager">
		SELECT * FROM
		(SELECT ROWNUM R,N.* FROM
		(SELECT * FROM PRODUCT P
				  LEFT JOIN
				  ACCOUNT A
				  USING(PRODUCTNUM)
		<include refid="search"></include>
		ORDER BY A.ACCOUNTBALNACE DESC)N)
		WHERE R BETWEEN #{startRow} AND #{lastRow}
	</select>
	
	<resultMap type="AccountDTO" id="getListResult">
		<id column="ACCOUNTNUM" property="accountNum"></id>
		<result column="USERNAME" property="userName"/>
		<result column="ACCOUNTPW" property="accountPw"/>
		<result column="ACCOUNTDATE" property="accountDate"/>
		<result column="ACCOUNTBLANACE" property="accountBlanace"/>
		<association property="productDTO" javaType="ProductDTO">
			<id column="PRODUCTNUM" property="productNum" ></id>
			<result column="PRODUCTNAME" property="productName"/>
			<result column="PRODUCTCONTENTS" property="productContents"/>
			<result column="PRODUCTRATE" property="productRate"/>
			<result column="PRODUCTJUMSU" property="productJumsu"/>
			<result column="PRODUCTCOUNT" property="productCount"/>
			<result column="PRODUCTSALE" property="productSale"/>
		</association>
	</resultMap>
		
	
	
	<select id="getTotalCount" resultType="Long">
		SELECT COUNT(ACCOUNTNUM) FROM ACCOUNT 			
	</select>
	

	<insert id="setJoin" parameterType="AccountDTO">
		INSERT INTO ACCOUNT
		(ACCOUNTNUM,USERNAME,PRODUCTNUM,ACCOUNTPW)
		VALUES(#{accountNum},#{userName},#{productNum},#{accountPw})
	</insert>
	
	<insert id="setWish" parameterType="AccountDTO">
		INSERT INTO WISHLIST 
		VALUES(#{productNum},#{userName};
	</insert>

</mapper>
