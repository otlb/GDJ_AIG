<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.winter.app.product.ReplyDAO">

	<delete id="setDelete" parameterType="ReplyDTO">
		DELETE REPLY WHERE REPLYNUM=#{replyNum}
	</delete>

	<update id="setUpdate" parameterType="ReplyDTO">
		UPDATE REPLY SET REPLYCONTENTS=#{replyContents}
		WHERE REPLYNUM=#{replyNum}
	</update>


	<insert id="setReply" parameterType="ReplyDTO">
		INSERT INTO REPLY
		(REPLYNUM,PRODUCTNUM ,USERNAME, REPLYCONTENTS, REPLYDATE, REPLYJUMSU)
		VALUES
		(REPLY_SEQ.NEXTVAL, #{productNum}, #{userName},#{replyContents}, SYSDATE,#{replyJumsu})
	</insert>


	<select id="getList" parameterType="Map" resultType="ReplyDTO">
		SELECT * FROM
		(SELECT ROWNUM RN,R.* FROM
		(SELECT * FROM REPLY WHERE PRODUCTNUM =
		#{replyDTO.productNum}
		ORDER BY REPLYNUM DESC)R)
		WHERE RN BETWEEN
		#{pager.startRow} AND #{pager.lastRow}
	</select>

	<select id="getTotalCount" parameterType="ReplyDTO"
		resultType="Long">
		SELECT COUNT(REPLYNUM) FROM REPLY WHERE PRODUCTNUM =
		#{productNum}
	</select>

</mapper>